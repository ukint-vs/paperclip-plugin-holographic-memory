node_modules/
dist/
coverage/
.turbo/
.DS_Store
*.log

# Personal sanity-check script tied to an unrelated project; intentionally
# untracked so it doesn't leak into npm or the public repo.
scripts/sanity-aperol.ts
manifest.json
